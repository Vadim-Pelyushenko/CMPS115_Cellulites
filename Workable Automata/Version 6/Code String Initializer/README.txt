Since we aren't able to have text files store our presets, we store them as strings,
this makes storing the initial state of the board really really annoying.

So this things takes a board's initial state, and turns it into code that
can save it as a string.
