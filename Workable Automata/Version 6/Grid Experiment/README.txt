Getting mouse clicks registered in the correct place and drawing a grid

Just try it out!
The resulting saved text that encodes the grid is put in the console,
you can then use the Code String Initializer to make it into a preset
